# 🔄 SYSTEM FLOW DIAGRAM

## User Journey: Creating a Store

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. USER INTERACTION                                             │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │   Dashboard UI   │
                    │  (React/Vite)    │
                    │  localhost:5173  │
                    └──────────────────┘
                              │
                              │ POST /api/stores
                              │ { name: "my-shop", type: "woocommerce" }
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. API LAYER                                                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                    ┌──────────────────┐
                    │   Backend API    │
                    │  (Express.js)    │
                    │  localhost:3000  │
                    └──────────────────┘
                              │
                              │ 1. Validate input
                              │ 2. Generate namespace: "store-my-shop-abc123"
                              │ 3. Insert into PostgreSQL
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. DATABASE                                                     │
└─────────────────────────────────────────────────────────────────┘
                              │
                    ┌──────────────────┐
                    │   PostgreSQL     │
                    │                  │
                    │ stores table:    │
                    │ - id: abc-123    │
                    │ - name: my-shop  │
                    │ - status: provisioning │
                    │ - namespace: store-my-shop-abc123 │
                    └──────────────────┘
                              │
                              │ Orchestrator polls every 10s
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. ORCHESTRATOR (Reconciliation Loop)                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                    ┌──────────────────┐
                    │  Orchestrator    │
                    │  (Node.js)       │
                    │                  │
                    │ Finds stores     │
                    │ with status=     │
                    │ 'provisioning'   │
                    └──────────────────┘
                              │
                              │ For each store:
                              ▼
                    ┌──────────────────┐
                    │ WooCommerce      │
                    │ Provisioner      │
                    └──────────────────┘
                              │
                              ├─ Step 1: Create namespace
                              │          kubectl create namespace store-my-shop-abc123
                              │
                              ├─ Step 2: Create ResourceQuota
                              │          kubectl apply -f resourcequota.yaml
                              │
                              ├─ Step 3: Install Helm chart
                              │          helm install my-shop ./woocommerce \
                              │            --namespace store-my-shop-abc123 \
                              │            --set storeName=my-shop \
                              │            --set domain=local.dev
                              │
                              ├─ Step 4: Wait for pods to be ready
                              │          kubectl wait --for=condition=ready pod \
                              │            -n store-my-shop-abc123
                              │
                              └─ Step 5: Update database
                                         UPDATE stores SET status='ready',
                                         url='http://my-shop.local.dev'
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. KUBERNETES RESOURCES CREATED                                │
└─────────────────────────────────────────────────────────────────┘

    Namespace: store-my-shop-abc123
    ├── ResourceQuota
    │   └── CPU: 2 cores, Memory: 4Gi, PVCs: 3
    │
    ├── MySQL StatefulSet
    │   ├── Pod: mysql-0
    │   │   └── Container: mysql:8.0
    │   ├── Service: mysql (ClusterIP)
    │   └── PVC: mysql-data-mysql-0 (10Gi)
    │
    ├── WordPress Deployment
    │   ├── Pod: wordpress-xxxxx
    │   │   ├── Init Container: wait-for-mysql
    │   │   └── Container: wordpress:latest
    │   ├── Service: wordpress (ClusterIP)
    │   └── PVC: wordpress-pvc (10Gi)
    │
    ├── Ingress
    │   └── Host: my-shop.local.dev → wordpress:80
    │
    └── Secret
        └── mysql-secret (root password, database name)

                              │
                              │ Pods become ready
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. STORE IS READY                                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Dashboard polls /api/stores
                              ▼
                    ┌──────────────────┐
                    │   Dashboard UI   │
                    │                  │
                    │ Status: READY ✅ │
                    │ URL: http://my-shop.local.dev │
                    │ [Open Store] [Delete] │
                    └──────────────────┘
                              │
                              │ User clicks "Open Store"
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 7. USER ACCESSES STORE                                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                    ┌──────────────────┐
                    │   Browser        │
                    │                  │
                    │ http://my-shop.local.dev │
                    └──────────────────┘
                              │
                              │ DNS: local.dev → 127.0.0.1
                              ▼
                    ┌──────────────────┐
                    │ nginx-ingress    │
                    │ (Kind cluster)   │
                    └──────────────────┘
                              │
                              │ Routes to wordpress service
                              ▼
                    ┌──────────────────┐
                    │ WordPress Pod    │
                    │                  │
                    │ Serves storefront│
                    └──────────────────┘
                              │
                              │ WordPress setup wizard
                              ▼
                    ┌──────────────────┐
                    │ User completes   │
                    │ WordPress setup  │
                    │ Installs WooCommerce │
                    │ Creates product  │
                    │ Places order ✅  │
                    └──────────────────┘
```

---

## Deletion Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ USER CLICKS DELETE                                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │   Dashboard UI   │
                    │                  │
                    │ Confirm deletion?│
                    │ [Yes] [No]       │
                    └──────────────────┘
                              │
                              │ DELETE /api/stores/abc-123
                              ▼
                    ┌──────────────────┐
                    │   Backend API    │
                    │                  │
                    │ UPDATE stores    │
                    │ SET status='deleting' │
                    └──────────────────┘
                              │
                              │ Orchestrator finds status='deleting'
                              ▼
                    ┌──────────────────┐
                    │  Orchestrator    │
                    │                  │
                    │ 1. helm uninstall my-shop │
                    │    --namespace store-my-shop-abc123 │
                    │                  │
                    │ 2. kubectl delete namespace │
                    │    store-my-shop-abc123 │
                    │                  │
                    │ 3. DELETE FROM stores │
                    │    WHERE id='abc-123' │
                    └──────────────────┘
                              │
                              │ All resources deleted
                              ▼
                    ┌──────────────────┐
                    │   Dashboard UI   │
                    │                  │
                    │ Store removed    │
                    │ from list ✅     │
                    └──────────────────┘
```

---

## Component Communication

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│  Dashboard  │────────▶│  Backend    │────────▶│ PostgreSQL  │
│  (React)    │  HTTP   │  API        │  SQL    │             │
│  Port 5173  │◀────────│  Port 3000  │◀────────│  Port 5432  │
└─────────────┘         └─────────────┘         └─────────────┘
                                                        │
                                                        │ Polls
                                                        ▼
                                                 ┌─────────────┐
                                                 │Orchestrator │
                                                 │  (Node.js)  │
                                                 └─────────────┘
                                                        │
                                                        │ kubectl/helm
                                                        ▼
                                                 ┌─────────────┐
                                                 │ Kubernetes  │
                                                 │   API       │
                                                 └─────────────┘
                                                        │
                                                        │ Creates
                                                        ▼
                                        ┌───────────────────────────┐
                                        │  Store Namespaces         │
                                        │  - store-shop1-abc        │
                                        │  - store-shop2-def        │
                                        │  - store-shop3-ghi        │
                                        └───────────────────────────┘
```

---

## Data Flow

```
CREATE STORE:
Dashboard → Backend → PostgreSQL (INSERT) → Orchestrator (poll) → Kubernetes (create) → PostgreSQL (UPDATE status)

LIST STORES:
Dashboard → Backend → PostgreSQL (SELECT) → Dashboard (render)

DELETE STORE:
Dashboard → Backend → PostgreSQL (UPDATE status='deleting') → Orchestrator (poll) → Kubernetes (delete) → PostgreSQL (DELETE)

AUTO-REFRESH:
Dashboard → setInterval(5000) → Backend → PostgreSQL → Dashboard (update UI)
```

---

## File Locations

```
DASHBOARD:     /home/sumit/urumi/dashboard/
BACKEND:       /home/sumit/urumi/backend/
ORCHESTRATOR:  /home/sumit/urumi/orchestrator/
HELM CHARTS:   /home/sumit/urumi/helm/store-templates/woocommerce/
SCRIPTS:       /home/sumit/urumi/scripts/
DOCS:          /home/sumit/urumi/*.md
```

---

## Ports & URLs

```
Dashboard:     http://localhost:5173
Backend API:   http://localhost:3000
PostgreSQL:    postgres-postgresql.platform.svc.cluster.local:5432
Store Example: http://my-shop.local.dev
```

---

## Key Commands

```bash
# Setup
./scripts/setup-local.sh

# Start components
cd backend && npm start
cd orchestrator && npm start
cd dashboard && npm run dev

# Check Kubernetes
kubectl get pods -A
kubectl get namespaces
kubectl get ingress -A

# Check store
kubectl get pods -n store-my-shop-abc123
kubectl logs -n store-my-shop-abc123 deployment/wordpress

# Cleanup
kind delete cluster --name store-orchestrator
```
